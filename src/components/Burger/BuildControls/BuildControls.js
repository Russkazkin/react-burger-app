import React from "react";
import classes from "./BuildControls.module.sass";

const buildControls = (props) => (
  <div className="w-full flex flex-col text-center shadow-sm m-auto py-2.5">

  </div>
);

export default buildControls;
